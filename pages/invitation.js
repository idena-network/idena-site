import Layout from '../shared/components/layout'

export default function Invitation() {
  return (
    <Layout
      title="How to join Idena Network"
      description="Invitations can be only sent out by validated nodes. Idena core team is granted to issue a limited number of invitations per month to support the network growth."
    >

    </Layout>
  )
}
